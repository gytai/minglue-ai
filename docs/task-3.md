## 目标

严格按照 Stage 1 契约与 Stage 2 数据模型，完成明略厂商回调接口的精确适配、幂等处理、状态更新、安全校验与审计查询。

## 前置与基线

- 先读取 Stage 1、Stage 2 的结果评论，从 Stage 2 报告的分支/提交继续开发。
- 只处理契约中有证据的字段和行为；文档未明确的内容保持兼容留存并在结果中标注，不得虚构协议。

## 交付与验收

- 回调路由、Content-Type、参数位置、成功响应和错误响应与 Apifox 文档逐项一致。
- 覆盖 `rec`、`op`、`sys`、`reclist`、`log`、`fc`、`asr`、心跳等类型，正确处理顶层/列表结构、单次多设备、事件时间和设备标识。
- 原始报文完整留存；重复回调可幂等识别；未知设备按明确策略建档；状态更新与日志落库位于合理事务边界，失败可追踪且不会部分提交。
- 若厂商文档定义签名/鉴权则严格实现；若未定义，现有可选 HMAC 只能作为本系统加固能力并明确兼容策略，不能冒充厂商标准。
- 增加请求级自动化测试，至少覆盖每类回调、重复事件、缺失/非法字段、多设备、签名错误、数据库异常与成功响应；不得只做函数级 smoke test。
- 提交并报告分支、提交号、契约覆盖情况、测试结果及仍需真实设备确认的事项。

## 串行交接

本阶段验收通过后，将本 issue 置为 `in_review`，再从父 issue 子任务列表中只提升 Stage 4 的唯一 `backlog` 子任务为 `todo`；不得提前启动其他阶段。
