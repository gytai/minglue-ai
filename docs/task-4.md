## 目标

完成设备管理后端与明略厂商控制接口的精确对接，包括设备台账、远端状态/配置同步、录音控制、指令结果查询及一致的错误处理。

## 前置与基线

- 先读取 Stage 1 至 Stage 3 的结果评论，从 Stage 3 报告的分支/提交继续开发。
- 使用 Stage 1 契约中的真实参数位置、字段名、鉴权与响应结构，不沿用未经验证的猜测。

## 交付与验收

- 完成设备新增、编辑、详情、删除、筛选、分页和状态字段维护，所有接口遵循现有登录与按钮权限体系。
- 对齐厂商登录、AES 密码处理、token 缓存/刷新、批量设备状态、配置状态、开始/停止录音、指令接收日志等接口；请求参数、认证头和响应解包与文档一致。
- 网络超时、401/403、厂商业务错误、非法响应、部分设备失败均有确定行为和可读错误，不泄露密码、token 或敏感报文。
- 远端同步与本地台账更新的一致性规则明确；批量操作不得因单项失败留下不可解释状态。
- 增加 mock 厂商服务的请求级/服务级测试，验证加密向量、token 刷新、重试边界、参数序列化、错误映射和本地更新；提交并报告分支、提交号及验证结果。

## 串行交接

本阶段验收通过后，将本 issue 置为 `in_review`，再从父 issue 子任务列表中只提升 Stage 5 的唯一 `backlog` 子任务为 `todo`；不得提前启动其他阶段。
