## 目标

在 Dash-FastAPI-Admin 中完成可用的设备管理、厂商控制和回调审计页面，并与后端权限、字段与错误语义保持一致。

## 前置与基线

- 先读取 Stage 1 至 Stage 4 的结果评论，从 Stage 4 报告的分支/提交继续开发。
- 复用现有组件、菜单、角色、操作日志和前端 API 封装风格，不引入第二套前端框架。

## 交付与验收

- 设备列表支持契约要求的搜索、分页、状态展示、详情、新增、编辑、删除和刷新；完整心跳字段在合适的列表/详情层级展示。
- 提供有权限控制的状态/配置同步、开始/停止录音、指令结果查询等交互；加载、成功、部分失败、超时和无权限反馈清晰，危险操作有确认。
- 回调日志支持按设备、类型、时间和处理结果查询，能查看格式化原始报文、幂等/处理信息，不在页面泄露密钥或 token。
- 菜单、角色与按钮权限在前端显示和后端校验两层一致；MySQL/PostgreSQL 种子数据均覆盖。
- 增加前端回调/组件或浏览器级测试，覆盖关键列表筛选、详情、控制操作、权限与错误反馈；提交并报告分支、提交号、页面清单和验证结果。

## 串行交接

本阶段验收通过后，将本 issue 置为 `in_review`，再从父 issue 子任务列表中只提升 Stage 6 的唯一 `backlog` 子任务为 `todo`；不得提前启动其他阶段。
