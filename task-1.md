## 目标

以父 issue GYTAI-123 的需求、带密码的 Apifox 文档和现有代码为唯一事实来源，完成接口契约盘点与当前实现差距分析，为后续开发建立可执行基线。

## 工作基线

- 当前主仓库 `main` 只有初始化内容；已有首版实现在本地分支 `agent/codex/gytai-123`，当前提交链包含 `3626af6` 与 `27ed2e8`。开始前先确认并把该分支内容纳入本任务分支，禁止从空白主分支重复开发或覆盖已有成果。
- 从父 issue 的 Apifox 地址及 2026-09-22 的密码评论访问文档；密码只用于访问，不得写入代码、配置、日志或提交。
- 保留 Dash-FastAPI-Admin 现有框架、目录与权限体系。

## 交付与验收

- 逐项整理登录/鉴权、设备查询与控制、回调事件的路径、方法、请求参数、响应结构、错误码、字段类型、必填性和示例。
- 覆盖 `rec`、`op`、`sys`、`reclist`、`log`、`fc`、`asr`、心跳等文档定义的回调类型；不凭字段名猜测。
- 将契约逐项映射到现有后端、前端、SQL 与测试，标记“已满足 / 部分满足 / 缺失 / 文档不明确”，并按 P0/P1/P2 排序。
- 在仓库新增或更新一份可供后续阶段引用的契约与差距文档，记录文档更新时间和仍需真实环境确认的事项；本阶段不做大规模业务代码改造。
- 执行必要的文档/代码静态检查，提交本阶段成果，并在 issue 评论中给出分支名、提交号、差距摘要和后续风险。

## 串行交接

本阶段验收通过后，将本 issue 置为 `in_review`，再读取父 issue `01a0c92f-9496-74a0-b919-390768a2404c` 的子任务列表，只把 Stage 2 的唯一 `backlog` 子任务提升为 `todo`；不得提前启动其他阶段。
