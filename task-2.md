## 目标

依据 Stage 1 产出的权威接口契约和差距矩阵，补齐设备、回调、控制审计所需的数据模型、DAO 与数据库迁移，为后续接口实现提供稳定持久化基础。

## 前置与基线

- 先读取父 issue 及 Stage 1 的完整结果评论，确认 Stage 1 已成功交付；从 Stage 1 报告的分支/提交继续开发，并保留 `agent/codex/gytai-123` 的已有实现。
- 如果 Stage 1 未交付、分支不可用或契约仍存在影响表结构的关键歧义，停止实现并在本 issue 标明阻塞，不得自行猜测。

## 交付与验收

- 对齐设备主数据、在线/绑定/录音状态、电量、信号、存储、固件、最近在线时间，以及文档明确的完整心跳字段。
- 对齐回调原始报文、事件类型、事件/业务唯一键、设备标识、事件时间、处理结果、来源 IP、重复标记和审计时间；原文可追溯且幂等键有明确唯一性策略。
- MySQL 与 PostgreSQL 初始化/迁移脚本保持等价、可重复执行或有明确升级路径，菜单和权限种子数据不产生重复记录。
- DAO、VO/DO、Service 边界遵循现有项目风格，事务与索引方案可解释；不得在数据库中保存 Apifox 密码或厂商明文凭证。
- 增加模型/DAO/迁移测试，验证唯一约束、字段兼容、重复回调和新旧库升级；提交并报告分支、提交号、变更表及验证结果。

## 串行交接

本阶段验收通过后，将本 issue 置为 `in_review`，再从父 issue 子任务列表中只提升 Stage 3 的唯一 `backlog` 子任务为 `todo`；不得提前启动其他阶段。
